// import { fromJS } from 'immutable';
// import { makeSelectAppIdDomain } from '../selectors';

// const selector = makeSelectAppIdDomain();

describe('makeSelectAppIdDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

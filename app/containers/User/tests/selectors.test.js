// import { fromJS } from 'immutable';
// import { makeSelectUserDomain } from '../selectors';

// const selector = makeSelectUserDomain();

describe('makeSelectUserDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

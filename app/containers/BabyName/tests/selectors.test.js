// import { fromJS } from 'immutable';
// import { makeSelectBabyNameDomain } from '../selectors';

// const selector = makeSelectBabyNameDomain();

describe('makeSelectBabyNameDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

// import { fromJS } from 'immutable';
// import { makeSelectPersonChooserDomain } from '../selectors';

// const selector = makeSelectPersonChooserDomain();

describe('makeSelectPersonChooserDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});

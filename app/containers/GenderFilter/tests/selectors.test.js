// import { fromJS } from 'immutable';
// import { makeSelectGenderFilterDomain } from '../selectors';

// const selector = makeSelectGenderFilterDomain();

describe('makeSelectGenderFilterDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
